One key learning was that the standard AWS S3 console requires the s3:ListAllMyBuckets permission to display bucket lists. Since this permission operates at the account level and cannot be restricted to specific buckets, IAM policies alone cannot make the console display only a single bucket. However, access to the contents of individual buckets can still be restricted using bucket-specific permissions.
